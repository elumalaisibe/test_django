{
    "builds": [{
        "src": "source/app/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "source/app/wsgi.py"
        }
    ]
}
